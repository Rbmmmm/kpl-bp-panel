# Hero Data Cache

Generated hero metadata and cached hero icons live under `public/data` and `public/heroes`.

The renderer loads the generated local `data/heroes.json` asset and does not fetch remote hero data at runtime.
